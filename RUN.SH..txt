#!/bin/bash

# Comando corrigido: Executa o alembic como um módulo do Python
python -m alembic upgrade head

# Comando corrigido: Executa o uvicorn como um módulo do Python
python -m uvicorn main:app --host 0.0.0.0 --port $PORT
